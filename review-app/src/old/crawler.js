const axios = require("axios");
const cheerio = require("cheerio");
/*
const getPostTitles = async () => {
    try {
      const { data } = await axios.get(
        "http://api.scrapestack.com/scrape/?access_key=e0a3dbce51a163a527db8cba3bcdbc25&url=http://black-up.kr/category/new-rising-item/302/"
      );
      const $ = cheerio.load(data);
      const postTitles = [];
  
      postTitles.push($(".thumbnail > a");
  
      return postTitles;
    } catch (error) {
      throw error;
    }
  };
  
  getPostTitles().then((postTitles) => console.log(postTitles));
  */
